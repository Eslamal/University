<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:layout_marginHorizontal="8dp"
    app:cardBackgroundColor="@color/card_background"
    app:cardCornerRadius="20dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <ImageButton
            android:id="@+id/btn_remove"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/bg_delete_soft"
            android:src="@android:drawable/ic_menu_delete"
            android:padding="8dp"
            android:scaleType="fitCenter"
            app:tint="@color/red_dark"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:contentDescription="Remove Course"/>

        <EditText
            android:id="@+id/et_course_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@drawable/rounded_bg"
            android:backgroundTint="@color/main_background"
            android:hint="@string/hint_subject_name"
            android:padding="14dp"
            android:drawableStart="@drawable/ic_book_edit"
            android:drawablePadding="12dp"
            android:drawableTint="@color/accent_color"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_secondary"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:inputType="textCapWords"
            app:layout_constraintEnd_toStartOf="@id/btn_remove"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginEnd="12dp"/>

        <EditText
            android:id="@+id/et_credits"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/rounded_bg"
            android:backgroundTint="@color/main_background"
            android:hint="@string/hint_credits"
            android:inputType="numberDecimal"
            android:padding="14dp"
            android:drawableStart="@drawable/ic_numeric"
            android:drawablePadding="8dp"
            android:drawableTint="@color/accent_color"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_secondary"
            android:textSize="14sp"
            app:layout_constraintEnd_toStartOf="@+id/spinner_container"
            app:layout_constraintHorizontal_weight="1"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/et_course_name"
            android:layout_marginEnd="12dp"/>

        <FrameLayout
            android:id="@+id/spinner_container"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="@drawable/rounded_bg"
            android:backgroundTint="@color/main_background"
            app:layout_constraintBottom_toBottomOf="@id/et_credits"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_weight="1"
            app:layout_constraintStart_toEndOf="@id/et_credits"
            app:layout_constraintTop_toTopOf="@id/et_credits">

            <Spinner
                android:id="@+id/spinner_grade"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:textAlignment="center" />
        </FrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>